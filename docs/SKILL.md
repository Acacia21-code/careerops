# CareerOps agent skill

Public-facing skill docs. Full mode prompts live in [`.agents/skills/careerops/`](../.agents/skills/careerops/).

```bash
npx @telivity/careerops init
```

Modes: `scan` · `evaluate` · `rank` · `tailor` · `interview` · `followup` · `outcome` · `advise`

Doctrine: materials only — never invent experience; never auto-apply. Bullet memory provenance + `resume_struct` canonical sync — see [DOCTRINE_MEMORY.md](DOCTRINE_MEMORY.md).

Export a **Board pack** (`schema_version` 2+) from the web app (Settings → Your data) for offline skill runs — includes accomplishments and portfolio.

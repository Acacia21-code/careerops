# Deploy (placeholder tokens only)

```bash
npx supabase functions deploy ai-free --project-ref YOUR_PROJECT_REF
# Access token example shape (never commit a real one): sbp_xxxxxxxx
# Set FREE_AI_ENDPOINT / FREE_AI_TOKEN / FREE_AI_MODEL / FREE_AI_ALLOW on the project.
```
